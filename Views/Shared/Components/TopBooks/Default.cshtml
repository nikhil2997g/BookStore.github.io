@model IEnumerable<BookModel>
<div class="row">
    @foreach (var book in Model)
    {
        <div class="col-md-4">
            <partial name="_bookThumbnail" model="book" />

            @*we can also use html partial*@
            @*@Html.Partial("_bookThumbnail", new BookModel());*@
        </div>
    }

</div>
